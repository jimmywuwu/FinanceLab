1:10 # 製造一個向量裡面的元素是1:10
sequence(10) # 製造一個向量裡面的元素是1:10
seq(1,9,2) #從1開始9為結束的等差數列形成的向量
c(1,3)

rbind(seq(1,9,2),seq(1,9,2)) #行合併 

cbind(seq(1,9,2),seq(1,9,2)) #列合併
rbind(1,3)
cbind(seq(1,9,2),seq(1,9,2))%*%rbind(1,3)

a=c(1,2,3,4,5,"k")


a=lm(seq(1,9,2)~seq(9,1,-2)) #lm(a~b)就是a對b做多元線性回歸，b可以是矩陣
summary(a) #將a的結果summay出來，不止可以放lm的結果、也可以放矩陣（會回傳敘述統計量等資訊）

plot(1:10,seq(2,20,2))
